@model RepairableModel
@{
    ViewData["Title"] = "Удалить модель";
}

<h2>Удалить модель автомобиля</h2>

<h3>Вы уверены, что хотите удалить эту модель?</h3>
<div>
    <h4>@Model.Manufacturer @Model.Name</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">Особенности:</dt>
        <dd class="col-sm-9">@Model.Features</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Отмена</a>
    </form>
</div>