@model FaultType
@{
    ViewData["Title"] = "Удалить неисправность";
}

<h2>Удалить вид неисправности</h2>

<h3>Вы уверены, что хотите удалить эту неисправность?</h3>
<div>
    <h4>@Model.Description</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">Стоимость:</dt>
        <dd class="col-sm-9">@Model.RepairCost.ToString("N2")</dd>
        <dt class="col-sm-3">Модель:</dt>
        <dd class="col-sm-9">@Model.Model?.Manufacturer @Model.Model?.Name</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Отмена</a>
    </form>
</div>